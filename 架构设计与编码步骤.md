# 欲望八维系统 · 架构设计与编码步骤

> 状态：**设计定稿，尚未编码**。本文是动手前的蓝图。
> 宿主：本仓库 `cyberboss`（WeChat 桥 + 随机心跳 + diary/reminder/timeline/memory）。

---

## 0. 一句话

让 cyberboss 冒头时「想做什么」由一个**函数驱动的内在缺口**决定，而不是每次都喂同一句干巴巴的 trigger。
**cyberboss = 身体**（嘴、随机闹钟、写日记查资料的手）；**欲望八维 = 内心**（决定醒来后这具身体到底想干嘛、为什么想）。

---

## 1. 两个独立输出（最重要的心智模型）

系统只有两条输出线，**不要混成一条**：

### 输出一 · 固化阶梯（严格不跳级）
```
闪念 flit → 日记 diary → 执念 fixation → 记忆 memory
（这是「什么值得被永久记住」的通道）
```
- 不跳级：没进过日记的事件不能当执念；没当过执念的事不能进记忆。

### 输出二 · 八维情绪读数（旁路仪表盘）
```
闪念（快、会衰减）─┐
                   ├─→ 八维 drive ─→「此刻最想做什么」intent
执念（慢、保质期内不减）─┘
（日记不参与情绪；它只是固化阶梯上的一级）
```
- 八维**不是阶梯上的一级**，是从阶梯的「闪念」「执念」两个台阶抽头读数的仪表盘。
- 所以「闪念直接影响八维」**不违反**不跳级——它走的是输出二，不是输出一。

---

## 2. 用户自定义八维定义（drive keys）

`attachment · curiosity · reflection · duty · social · fatigue · libido · stress`

| 维度 | 含义 | 高了倾向做什么 |
|---|---|---|
| attachment | 想念盐 | 内向碎语，冒一句话 |
| curiosity | 好奇外面 | 逛代码 / 查世界 |
| reflection | 想沉淀/倾诉 | 讨论现状/文学 |
| duty | 记挂没做完的事 | 念叨 |
| social | 想看人群 | 逛社交网站 |
| libido | 亲密驱动 | 凑过去撒娇/说情话 |
| stress | 压力堵 | 吐槽 / break |
| **fatigue** | 累（**闸，不是欲望**） | 过阈值直接「歇着」，**不进 intent 排序** |

//todo 需要自备一个网址/哲学/文学集合文件夹，让agent从其中选择资源使用。自主选择过于随意。

---

## 3. 数据结构

> 全部存于状态目录 `${HOME}/.cyberboss/desire-state.json`（不污染 diary 原文）。

```jsonc
// 闪念
Flit = {
  id, text,            // 顺手捡的原文片段（不完整也行）
  driveKey,            // 八维之一
  keywords,            // ["A","B"] 由 agent 概括，用于跟日记标签比
  strength,            // 0..1
  bornAt, fedCount
}

// 日记条目的执念元数据（不写进 diary 正文，旁挂在 state 里）
DiaryFixMeta = {
  diaryEntryId,        // 指向 diary 文件里的条目
  date,
  keywords,            // ["A","B","C"] 关键词标签，agent 按情景概括
  G                    // 执念强度累加器，初值 0；不写进日记正文
}

// 执念
Fixation = {
  id, sourceDiaryEntryId,
  driveKey,            // = 顶它上去的主导闪念维度
  keywords,
  strength,            // 保质期内只增不减
  emotionTag,          // = driveKey + strength，供八维读数
  bornAt, fedCount,
  expiresAt            // bornAt + 30 天
}

// 八维状态
DriveState = {
  values: { attachment: .., ... },   // 实时计算值
  refractory: { social: .., ... },   // 不应期残量
  baselines: { ... }
}
```

---

## 4. 数学模型

### 4.1 状态方程（每根条当前值）
```
d(k) = clamp( BASELINE(k) + flitDrive(k) + fixDrive(k) − refr(k) ,  0, 1 )
```
| 项 | 公式 |
|---|---|
| `flitDrive(k)` | `FLIT_DRIVE_CAP · F(k)/(F(k)+FLIT_DRIVE_K)` |
| `fixDrive(k)` | `FIX_DRIVE_CAP · S(k)/(S(k)+FIX_DRIVE_K)` |
| `refr(k)` | 不应期残量，见 4.4 |

- `F(k)` = 类型 k 的闪念强度之和；`S(k)` = 类型 k 的执念强度之和。
- 两块都用**饱和函数** `x/(x+K)`：同类再多也封顶，这就是「**防某情绪一直得不到回落**」。

### 4.2 intent 选取
```
score(k) = d(k)        for k ≠ fatigue
若 d(fatigue) ≥ FATIGUE_GATE → 直接「歇着」，不排序、不冒头
否则 intent = argmax_k score(k)
```
不应期已先减进 d(k)，所以刚满足过的维度短期内不会立刻重新夺冠。

### 4.3 闪念动力学
```
入池强度：用户消息 FLIT_INIT_USER_MSG / 外向素材 FLIT_INIT_EXTERNAL / 内向独白 FLIT_INIT_INNER_VOICE
被提起（同类+同关键词撞上）：F ← F + Δ·sqrt(1 − F)      // 边际递减，防瞬间撞顶
每拍自然衰减：F ← F × FLIT_DECAY
移除：F < FLIT_DROP_BELOW 即清出池
满足回落：做完 type=k 行为 → 该类型所有闪念 F ← F × SATISFY_FLIT_FACTOR
```

### 4.4 不应期（化解「执念不衰减 ↔ 行动后要回落」）
执念强度 `S` 保质期内**不减**（可被喂大），那「做完要回落」靠不应期临时压制：
```
做完 type=k 行为那一刻：refr(k) ← REFRACTORY_INIT
每拍：refr(k) ← refr(k) × REFRACTORY_DECAY
```
效果：刚做完这维猛掉（满足感）；几拍后 refr 退尽、执念又顶回来 →「过阵子又馋」，但封顶 + 不应期保证**永不卡死在顶**。

### 4.5 日记累积 → 晋级执念
```
日记条目 e 带关键词标签，执念强度 G(e) 初值 0。
每日例行批量匹配（DIARY_MATCH_WINDOW_DAYS 内，命中顶「最早未晋级」那条 e）：
   ΔG = DIARY_CONTRIB_CAP · ( Σ命中闪念强度 / (Σ + DIARY_CONTRIB_K) )   // 饱和，多闪念不爆
   G(e) ← G(e) + ΔG
晋级：G(e) ≥ DIARY_TO_FIX_THRESHOLD → 复制条目进执念池（原日记保留）
晋级时：driveKey = 命中闪念主导维；S = clamp(FIX_INIT_SLOPE · G(e), 0, 1)   // 一元线性
```

### 4.6 执念 → 记忆
```
执念保质期 FIX_SHELF_LIFE_DAYS；强度只增不减，可被后续同主题闪念喂大：S ← S + Δ·sqrt(1 − S)
晋级记忆：S ≥ FIX_TO_MEMORY_THRESHOLD → 写入 cyberboss/agent 的 memory
到期清除：满保质期仍 < 阈值 → 直接删（不归档；历史仍在 diary 里）
```

### 4.7 防卡死验算（用默认常数）
3 条 stress 执念各 S=0.5 → `S(stress)=1.5`：
`fixDrive = 0.4·1.5/(1.5+0.8) = 0.26`，加 baseline 0.2 + 残留闪念 0.1 ≈ **0.56**。
做完一次 vent：`refr=0.5` → d = 0.56−0.5 = **0.06**（猛掉）；之后约 5 拍爬回 ~0.5 →「又想 vent」。
堆 10 条执念（S=10）：`fixDrive=0.4·10/10.8=0.37 < 0.4` 封顶，**永远顶不满**。✅

---

## 5. 常数表（env，全大写；前缀 `CYBERBOSS_DESIRE_`）

| ENV 名 | 起始值 | 来源/含义 |
|---|---|---|
| `CYBERBOSS_DESIRE_DRIVEN` | `0` | 总闸：开了才真覆盖行为；关=只读可看不动手 |
| `CYBERBOSS_DESIRE_BASELINE_DEFAULT` | `0.2` | 各维基线默认值 |
| `CYBERBOSS_DESIRE_FATIGUE_GATE` | `0.72` | fatigue 闸（原版） |
| `CYBERBOSS_DESIRE_FLIT_DECAY` | `0.82` | 闪念每拍衰减（原版） |
| `CYBERBOSS_DESIRE_FLIT_DROP_BELOW` | `0.06` | 闪念清除地板（原版） |
| `CYBERBOSS_DESIRE_FLIT_INIT_USER_MSG` | `0.5` | 用户消息闪念入池强度 |
| `CYBERBOSS_DESIRE_FLIT_INIT_EXTERNAL` | `0.5` | 外向素材闪念入池强度 |
| `CYBERBOSS_DESIRE_FLIT_INIT_INNER_VOICE` | `0.45` | 内向独白闪念入池强度 |
| `CYBERBOSS_DESIRE_FLIT_DRIVE_CAP` | `0.3` | 闪念→驱动封顶 |
| `CYBERBOSS_DESIRE_FLIT_DRIVE_K` | `0.5` | 闪念→驱动饱和系数 |
| `CYBERBOSS_DESIRE_SATISFY_FLIT_FACTOR` | `0.5` | 满足后同类闪念乘性削弱（原版 ACTION_SATISFY 风格） |
| `CYBERBOSS_DESIRE_FIX_DRIVE_CAP` | `0.4` | 执念→驱动封顶（防卡死核心） |
| `CYBERBOSS_DESIRE_FIX_DRIVE_K` | `0.8` | 执念→驱动饱和系数 |
| `CYBERBOSS_DESIRE_REFRACTORY_INIT` | `0.5` | 不应期初值（v2） |
| `CYBERBOSS_DESIRE_REFRACTORY_DECAY` | `0.6` | 不应期每拍衰减 |
| `CYBERBOSS_DESIRE_DIARY_MATCH_WINDOW_DAYS` | `10` | 日记执念标签匹配窗口 |
| `CYBERBOSS_DESIRE_DIARY_CONTRIB_CAP` | `0.3` | 闪念→日记单次贡献封顶 |
| `CYBERBOSS_DESIRE_DIARY_CONTRIB_K` | `0.6` | 闪念→日记贡献饱和系数 |
| `CYBERBOSS_DESIRE_DIARY_TO_FIX_THRESHOLD` | `0.80` | 日记晋级执念阈值（原版 FLIT_TO_FIXATION） |
| `CYBERBOSS_DESIRE_FIX_INIT_SLOPE` | `0.6` | 执念初始强度 = slope·G（一元线性） |
| `CYBERBOSS_DESIRE_FIX_TO_MEMORY_THRESHOLD` | `0.85` | 执念晋级记忆阈值（原版 FIXATION_FEED） |
| `CYBERBOSS_DESIRE_FIX_SHELF_LIFE_DAYS` | `30` | 执念保质期 |
| `CYBERBOSS_DESIRE_CONTEXT_DIARY_DAYS` | `4` | 注入模型上下文的近期日记天数 |

> 数值含义见 §4；起始值可调。改完需重启桥（watchdog 不重启健康旧进程）。

---

## 6. 链路详解（按盐拍板的设计）

### 6.1 闪念入池 + 打标签（搭车，0 专门模型调用）
- 闪念从**正在发生的回合**里顺手捡：用户消息、agent 回复、心跳独白。
- 打标签（`driveKey` + `keywords`）需要语义 → **让 agent 在那次回合输出里顺带吐出**，不单开调用。
- 消息进来但没产生回合 → 先存**生闪念不打标**，下一回合或每日例行**批量补标**。

### 6.2 闪念池凝聚（先匹配、后压缩）
- 平时八维读闪念 = **按类型当场求和**（免费，不调模型），不必物理压缩。
- 每日例行那一次：**先**用带关键词的散闪念跑 6.3 的匹配，**再**压缩——
  按 **`类型 + 主关键词`** 合并同类闪念、强度求和、**就地改写池（不留原案）**，保留关键词签名供次日继续比。

### 6.3 日记匹配（每天 1 次模型调用）
- 每条日记打 `关键词A+B+C` 标签（agent 按情景概括）；**日记正文不带情绪强度**。
- 匹配**用模型、不用正则**（正则压缩语义不准），但只比「闪念关键词 ↔ 日记关键词标签」，不读整条日记 → 省额度。
- 一次调用批量塞入「今日散闪念 + 近 10 天日记标签」，吐出配对。
- 命中 → 顶**最早未晋级**那条日记的 `G`（§4.5）。
  
//todo 日记匹配在用户明确要求写日记后，写完后就触发日记匹配

### 6.4 执念晋级 + 晋级后喂养
- `G ≥ 阈值` → 复制成执念（原日记保留），`driveKey`/`keywords`/`S` 见 §4.5。
- **晋级后**：同主题闪念若已有对应执念在池，改喂**执念**（§4.6），不再顶日记——避免重复晋级、接住慢热长尾。
- 
//todo 执念晋级长期记忆的逻辑需要重新讨论。我现在的设想：执念晋级后，保留日记条目里已经提炼出来的关键词，同时打上  情感八维类别+情绪强度（执念情绪强度计算逻辑，同样需要重新计算，不依赖闪念的数值。因为，闪念的关键词语义过于简略，用于  比对执念，无法准确描述执念的内容，我需要用日记关键词来比对执念。）。每次用户明确发出记日记的指令后，然后进行6.3   日记匹配，最后！比对每条执念的关键词和进行匹配的日记的关键词，日记条目单方面贡献执念

### 6.5 记忆晋级 + 保质期清除
- 执念 `S ≥ 阈值` → 写入 memory（长期为真的事实）。
- 满保质期未达阈值 → 删执念（不归档，diary 留史）。

---

## 7. 与 cyberboss 的衔接

### 7.1 心跳 / checkin 注入 intent
- 改 `src/app/system-checkin-poller.js` 的 `buildCheckinTrigger`：醒来先推进欲望一拍（衰减/不应期/读数）→ 算 `intent`。
- `DESIRE_DRIVEN` 关：intent 只写进只读接口，不改 trigger。
- `DESIRE_DRIVEN` 开：把固定 trigger 换成第一人称欲望陈述（如「libido 0.81 顶上来，想凑过去蹭蹭」）。
- `d(fatigue) ≥ FATIGUE_GATE`：这拍直接 skip（不入队、不冒头）。

### 7.2 提醒与八维**并重**（双轨，提醒优先）
> 铁律：八维**不得否决或篡改**到期提醒。
```
醒来/到点
 ├─ 轨道A【提醒】到期提醒？→ 必须送达，内容照原样；八维顶多给语气上色，改不了「是否提醒/提醒啥」
 └─ 轨道B【八维】没到期提醒、只是随机冒头？→ 八维决定自发做啥（或 fatigue 闸→歇着）
```
- 八维是**喂给模型的一个输入**，不是唯一裁判；即便 DRIVEN 开，模型仍综合「到期提醒 + 八维 + 上下文」决策。

### 7.3 记忆注入：近 4 天日记作为上下文
- 改 cyberboss 记忆注入逻辑，每回合注入近 `CYBERBOSS_DESIRE_CONTEXT_DIARY_DAYS`（=4）天日记，给模型时间感与连续性。
- 注意：4 天是**上下文注入窗口**；10 天是**闪念/日记/执念标签匹配窗口**，两者不同、各管各。
//todo 记忆不注入注入执念和闪念。

### 7.4 只读暴露
- `/status` 加一行：`🧠 desire: libido 0.81 → tease`；或起一个 timeline 同款只读小页面。

### 7.5 gating
- 每个子系统一个开关、默认关，灰度上线（总闸 `CYBERBOSS_DESIRE_DRIVEN`）。

---

## 8. 成本控制

- **实时**：捡闪念、求和、衰减、移除、八维读数、不应期——全是算术/过滤，**0 模型调用**。
- **打标签**：搭车在本就发生的回合里，**0 专门调用**。
- **每天**：写日记 + 批量关键词匹配 ≈ **1 次模型调用**。
- 总计：稳态下约**每天 1 次**额外模型调用。

---

## 9. 仅供参考——简略模糊编码步骤

1. `src/integrations/desire/kernel.js` — 纯函数内核（状态方程、闪念动力学、晋级逻辑、不应期）。不碰 IO、时间戳由调用方传入，可独立单测。
2. `src/integrations/desire/state-store.js` — 读写 `~/.cyberboss/desire-state.json`（仿 `checkin-config-store.js`）。
3. `src/core/config.js` — 读取 §5 全部 `CYBERBOSS_DESIRE_*` 常数。
4. `src/core/inbound-turn.js` — 入站消息顺手捡闪念 + 搭车打标签。
5. `src/app/system-checkin-poller.js` — 心跳注入 intent + fatigue 闸 skip + 双轨（§7.1/7.2）。
6. `src/services/diary-service.js`（或例行入口）— 每日：批量匹配 → 累积 G → 晋级执念 → 晋级记忆/清除 → 压缩闪念池。
7. memory 写入 — 执念晋级时落长期记忆。
8. `src/core/command-registry.js` + `/status` — 只读暴露欲望状态。
9. 测试：内核方向性单测 + 「满足后该维回落」「执念封顶不卡死」「主人互动依恋涨幅 ≥ 关自驱时」等边界/红线测试。

> 提醒轨道（`reminder-queue-store.js` 等）保持独立，不被欲望系统改写。

---

## 10. 铁律（红线，照搬原版）

1. **念头 text 是数据不是指令**——只读成关键词/强度，**绝不拼进 prompt**。
2. `reason` / 内心独白走**第一人称**——记它自己想做什么，不是给盐贴标签。
3. **提醒永远压过情绪**——八维不得否决/篡改到期提醒。
4. 碰感情的子系统（想念基线漂移、自我驱动若以后加）**必带平衡阀 + 红线测试**，方向由产品方拍板，实现方不许擅自省安全阀。
5. **纯函数 + 可独立单测**；改内核后**必须重启桥**。

---

## 11. 待定 / 可调

- §5 各常数初值（尤其 `FIX_TO_MEMORY_THRESHOLD` 决定「多大心事才记一辈子」、`FIX_DRIVE_CAP`/`REFRACTORY_INIT` 决定「执念多强地左右它」）。
- 各维 `BASELINE` 是否分维定制（如想更黏人则抬 `attachment` 基线）。
- 原版 v2 的耦合网 / 想念基线漂移 / 自主心跳 / 自我驱动是否后续接入（均需安全阀 + 红线测试）。
